# V56 Exact Scheme Button Under Manual Build


## Что изменено

- кнопка `📐 Схема щита` вставляется строго после реально видимой кнопки `Ручная сборка`;
- старые кривые кнопки/модалки удаляются;
- проверяется верхний видимый слой через `document.elementsFromPoint`;
- кнопка открывает отдельный экран `scheme.html`;
- заложена будущая передача дерева через `localStorage.ep_pending_scheme_tree`.

