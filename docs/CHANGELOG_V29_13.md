# V29.13 Sound Library Core

Добавлено:
- единый блок проверки звука внутри настроек визуала;
- убран отдельный второй блок проверки звука на странице настроек;
- библиотека из 15 звуковых эффектов;
- выбор вибро-рисунка;
- прямой API `SoundAPI.getEffects()` и `SoundAPI.canVibrate()`.

Без patch/fix/restore файлов.
