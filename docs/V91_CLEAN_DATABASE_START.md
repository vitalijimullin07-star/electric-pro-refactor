# V91 — Clean Database Start


- восстановлен главный экран из backup;
- старые рабочие JS больше не режутся;
- старые database JS удалены;
- старые страницы базы удалены;
- все старые входы `openMatCatalog`, `openWorkCatalog`, `renderDbEditors` и т.д. перекидываются на `database-v91.html`;
- создано чистое ядро `EP_DB_STATE_V91`;
- новая база пустая.


V92 — новая «Моя база» с нуля.
V93 — новая «База сервера».
V94 — копирование Сервер → Моя.
V95 — привязка `+ Материалы` / `+ Работа` к новой базе.
