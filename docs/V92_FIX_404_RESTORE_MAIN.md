# V92 — Fix 404 Restore Main


- восстановлен `public/index.html`;
- главный экран возвращён из backup;
- старые database JS удалены как отдельные файлы;
- рабочие JS больше не режутся;
- создано чистое ядро `EP_DB_STATE_V92`;
- старые входы базы переводятся на `database-v92.html`.


V93 — новая «Моя база» с нуля.
