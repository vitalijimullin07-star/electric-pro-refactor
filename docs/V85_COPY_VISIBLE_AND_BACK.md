# V85 Copy Visible And Back Fix


1. Кнопка `Назад` в `my-database.html` теперь использует `history.back()`, а не перезапускает `index.html`.
2. Копирование из базы сервера в мою базу теперь берёт данные напрямую из видимых выбранных строк.
3. Сохранение идёт в новую базу V83.


- выбрать позиции в базе сервера;
- нажать `Добавить выбранные в мою базу`;
- должно быть больше 0;
- открыть мою базу;
- обновить страницу;
- позиции должны остаться.
