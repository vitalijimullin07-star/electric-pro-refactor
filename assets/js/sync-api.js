(function(){let timer=null;async function syncOnce(){try{const changes=await window.ChangeLogAPI?.list?.()||[],pending=changes.filter(ch=>ch.syncStatus==="pending"),compacted=window.ChangeLogAPI?.compact?.(pending)||[];await window.CacheAPI?.set?.("last_sync_info",{time:new Date().toISOString(),pendingCount:pending.length,compactedCount:compacted.length,mode:"prepared-only"});window.Diagnostics?.sync?.(pending.length?"есть изменения: "+pending.length:"актуально")}catch{window.Diagnostics?.sync?.("ошибка")}}function start(){if(timer)return;syncOnce();timer=setInterval(syncOnce,60000)}function stop(){if(timer)clearInterval(timer);timer=null}window.SyncAPI={start,stop,syncOnce}})();
