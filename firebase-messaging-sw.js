/* Страховочный service worker для FCM.

   Обычно он НЕ НУЖЕН: клиент (assets/js/modules/ui/feedback.js, registerPush) передаёт
   в getToken СВОЮ регистрацию /sw.js, и push приходит туда. Но SDK при отсутствии
   переданной регистрации сам лезет за «дефолтным» /firebase-messaging-sw.js — а
   hosting на любой НЕсуществующий путь отдаёт index.html (rewrite "**" → /index.html),
   из-за чего SDK падал с «The script has an unsupported MIME type ('text/html')»
   (реальный лог с устройства пользователя). Теперь этот путь существует и является
   валидным скриптом — даже если какая-то ветка когда-нибудь снова уйдёт в дефолт,
   получится рабочий SW, а не ошибка.

   importScripts("/sw.js") вместо копии обработчиков — чтобы логика показа уведомления
   (в т.ч. «окно открыто и видно → не дублировать, его рисует страница») жила В ОДНОМ
   месте и не разъезжалась между двумя файлами. Побочные обработчики sw.js (кэш, fetch)
   в этой области видимости безвредны: у скоупа /firebase-cloud-messaging-push-scope
   нет собственных страниц, через него ничего не грузится. */
"use strict";
importScripts("/sw.js");
