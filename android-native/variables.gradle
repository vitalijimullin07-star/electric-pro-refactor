ext {
    minSdkVersion = 24
    compileSdkVersion = 36
    targetSdkVersion = 36
    androidxActivityVersion = '1.11.0'
    androidxAppCompatVersion = '1.7.1'
    androidxCoordinatorLayoutVersion = '1.3.0'
    androidxCoreVersion = '1.17.0'
    androidxFragmentVersion = '1.8.9'
    coreSplashScreenVersion = '1.2.0'
    androidxWebkitVersion = '1.14.0'
    junitVersion = '4.13.2'
    androidxJunitVersion = '1.3.0'
    androidxEspressoCoreVersion = '3.7.0'
    cordovaAndroidVersion = '14.0.1'
    // Вход через Google в @capacitor-firebase/authentication. По умолчанию false, и
    // тогда библиотеки Google Sign-In подключаются как compileOnly — то есть в APK
    // их нет вовсе, а вход падает уже на устройстве. Нужны обе половины: этот флаг
    // и providers:["google.com"] в capacitor.config.json.
    rgcfaIncludeGoogle = true
}